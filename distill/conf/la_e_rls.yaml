hydra:
  run:
    dir: outputs/la_e_rls/${now:%Y-%m-%d}/${now:%H-%M-%S}

wandb:
  project: "one-pass-distill"
  entity: null
  name: "LA-E-RLS_${now:%Y-%m-%d_%H-%M-%S}"
  mode: "online"

data:
  batch_size: 64
  num_workers: 24
  dataset_name: cifar100

model:
  name: resnet20
  num_classes: 100

train:
  epochs: 240
  lr: 0.05
  momentum: 0.9
  weight_decay: 0.0001
  
la_e_rls:
  alpha_min: 0.1
  alpha_max: 0.2
  h_min_ratio: 0.2 # If > 0, overrides h_min (Relative to log(C))
  h_max_ratio: 0.8 # If > 0, overrides h_max
  h_min: 0.5
  h_max: 3.5
  tau_min: 0.1
  tau_max: 5.0
  lambda_val: 0.5
  tau_kd: 1.0
  power_law_exp: 2.0
  distribution_type: "dirichlet" # Options: "power_law", "dirichlet"
  dirichlet_beta: 0.1 # 0.0 or negative -> Auto 1/C (Ideal for sparsity)
  bs_iters: 15
  seed: 42
  sorting_mode: "fully_sorted" # Options: "fully_sorted", "gt_anchored"
  lambda_schedule: "adaptive" # Options: "cosine", "adaptive"
  ema_decay: 0.99 # Decay rate for LossEMA
  dry_run: false
  